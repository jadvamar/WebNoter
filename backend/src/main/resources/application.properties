spring.application.name=webNoter

# Server port
server.port=8085

# MongoDB configuration
spring.data.mongodb.uri=mongodb://localhost:27017/webnoter

# JWT and security properties (optional, if JWT is used)
jwt.secret=your_jwt_secret_key
jwt.expiration=86400000  

